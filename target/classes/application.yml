spring:
  datasource:
    url: jdbc:mysql://123.57.92.221:3306/medical_imaging?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.your-email-provider.com
    port: 587
    username: your-email@example.com
    password: your-email-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

orthanc:
  server-url: http://123.57.92.221:8042
  username: orthanc
  password: orthanc

server:
  port: 8080

# 文件上传配置
spring.servlet.multipart:
  max-file-size: 100MB
  max-request-size: 100MB 

jwt:
  secret: abcdefg
  expiration: 86400  # 24小时 

# 备份配置
backup:
  directory: /data/backups
  retention-days: 30
  schedule: "0 0 1 * * ?"  # 每天凌晨1点 

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  show-actuator: true
  packages-to-scan: com.medical.imaging.controller 